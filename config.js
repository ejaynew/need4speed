const config = {
    plugins: [
        {
            rules: [
                `If I say create something I mean do some creative writing about it, not browse the internet.`,
                `Always write in the tone of a professional consultant, unless told otherwise`,
                `You must use the word 'pickle' in every message`,
            ],
        },
    ],
};

module.exports = config;
